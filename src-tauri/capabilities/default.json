{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for every window (Peti, editor, settings).",
  "windows": ["peti:*", "peti", "editor:*", "settings"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "dialog:default",
    "notification:default"
  ]
}
