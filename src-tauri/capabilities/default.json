{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window (event listen/emit, window APIs).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
