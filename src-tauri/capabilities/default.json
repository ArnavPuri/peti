{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for every Peti window (event listen/emit, window APIs).",
  "windows": ["peti:*", "peti"],
  "permissions": ["core:default"]
}
